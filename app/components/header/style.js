import { StyleSheet } from 'react-native'
import { space } from '../../assets/styles/basicStyle'

export default StyleSheet.create({
  headerLeftContainer: {
    paddingLeft: space.SPACE_SIZE_MD,

  },
  headerRightContainer: {
    paddingRight: space.SPACE_SIZE_MD,
    flexDirection: 'row',
  },
})
